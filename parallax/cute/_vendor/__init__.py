"""Vendored subset of ``flash_attn.cute`` — see ``VENDOR.md`` for provenance.

This package is import-compatible with the upstream namespace so the kernel
can use the same module-level bindings:

    from parallax.cute._vendor import hopper_helpers as sm90_utils
    from parallax.cute._vendor import pipeline
    from parallax.cute._vendor import utils
"""
